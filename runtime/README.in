# App @VERSION@

This is a sample multi-interface command-line and GUI Java application.

## Invokation

* Windows

Run `app` or `appfx` in a windows terminal for CLI or GUI version, respectively.
Run `App` from the Start menu for GUI version.

* Linux

_Make the `.AppImage` file is made executable with `chmod +x *.AppImage`._
Run an integrated `AppImage` executable for the CLI version and `AppImage -g` for the GUI version.
