plugins {
    id 'application'
    id 'buildlogic.java-common-conventions'
}

dependencies {
    implementation project(':app')
}

application {
    mainClass = 'app.cli.Main'
    mainModule = 'app.cli'
}